import React from 'react';

const About = () => {
    return (
        <div className="row">
            <br />
            
            <p className="col-md-12 about"><code>Search for the weather all over the US:</code>Middleware is the suggested way to extend Redux with custom functionality. Middleware lets you wrap the store's dispatch method for fun and profit. The key feature of middleware is that it is composable. Multiple middleware can be combined together, where each middleware requires no knowledge of what comes before or after it in the chain.</p>
        </div>
    )
}

export default About;